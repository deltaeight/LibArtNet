language: java
install: true

jdk:
  - openjdk8

stages:
  - test
  - name: deploy
    if: branch = master

jobs:
  include:
    - stage: test
      script: ./gradlew check
    - stage: deploy
      script: ./gradlew publish